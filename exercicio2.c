#include <stdio.h>
#include <stdlib.h>
#include <math.h>

	int main()
		{
int i, c, resultado;
printf("Enter first number:");
scanf("%d", &i);
printf("Enter second number:");
scanf("%d", &c);
resultado = i + c;
printf("o resultado e %d", resultado);
return 0;
	}